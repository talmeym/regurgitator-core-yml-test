sequence:
  steps:
    - create-parameter:
        name: name
        source: context:location
        processor:
          set-at-index-processor:
            index-source: "param-1"
            value-source: "param-2"